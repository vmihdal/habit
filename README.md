## Habit

## How to start

Backend:

```bash
cd backend
docker compose up -d
npm install
npm run start:dev
```

Frontend:

```bash
cd front
npm install
npm run dev
```

Open [http://localhost:5174/]( http://localhost:5174/) in browser.